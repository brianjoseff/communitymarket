%h1 Editing group
-if current_user && @group.owner == current_user || current_user.admin?
  = render 'form'

  = link_to 'Show', @group
  \|
  = link_to 'Back', groups_path
-else
  %p you are not the admin for this group
  =link_to "Go back to group page", group_path(@group)

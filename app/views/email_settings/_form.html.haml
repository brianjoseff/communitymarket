= form_for @email_setting do |f|
  - if @email_setting.errors.any?
    #error_explanation
      %h2= "#{pluralize(@email_setting.errors.count, "error")} prohibited this email_setting from being saved:"
      %ul
        - @email_setting.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'

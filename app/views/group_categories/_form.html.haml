= form_for @group_category do |f|
  - if @group_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@group_category.errors.count, "error")} prohibited this group_category from being saved:"
      %ul
        - @group_category.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'

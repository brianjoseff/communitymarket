.row
  .col-lg-3
  .col-lg-6
    %h1 New post
    .page-element-background
      =render 'form'
    / -if signed_in?
    /   = render 'form'
    / -else
    /   = render 'nonuser_form'

    = link_to 'Back', posts_path

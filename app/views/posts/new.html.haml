.row
  .col-lg-3
  .col-lg-6
    -if @post.sseller_post
      %h1 New <span id='super-seller'>SuperSeller</span> Post
    -else
      %h1 New post

    .page-element-background
      =render 'form'
    / -if signed_in?
    /   = render 'form'
    / -else
    /   = render 'nonuser_form'

    = link_to 'Back', posts_path
